syntax = "proto3";

package meda_manager;

import "fileslist.proto";
import "unrar.proto";

message Request {
    oneof type {
        media_manager.fileslist.Request fileslist = 1;
        media_manager.unrar.Request unrar = 2;
    }
}

message Response {
    oneof type {
        media_manager.fileslist.Response fileslist = 1;
        media_manager.unrar.Response unrar = 2;
    }
}
